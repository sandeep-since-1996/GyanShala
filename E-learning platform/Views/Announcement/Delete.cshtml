@model E_learning_platform.Models.AnnouncementDeleteViewModel

@{
    ViewBag.Title = "Delete Announcement";
}

<h2>Announcement</h2>

<h3>Delete</h3>
<div>
    <h4>Are you sure you want to delete this announcement?</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.CourseId)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back", "View", "Course", new { id = Model.CourseId }, null)
        </div>
    }
</div>
